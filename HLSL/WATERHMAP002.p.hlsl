//
// Generated by Microsoft (R) D3DX9 Shader Compiler 9.08.299.0000
//
//   psa shaderdump19/WATERHMAP002.pso /Fcshaderdump19/WATERHMAP002.pso.dis
//
//
// Parameters:

float fPassNum;
sampler2D sampButterfly;
sampler2D sampSourceImage;

// Structures:

struct VS_OUTPUT {
    float2 texcoord_0 : TEXCOORD0;
};

struct PS_OUTPUT {
    float4 color_0 : COLOR0;
};

// Code:

PS_OUTPUT main(VS_OUTPUT IN) {
    PS_OUTPUT OUT;

    float2 p0, p1, p2;

    p1.x = IN.texcoord_0.y;
    p1.y = fPassNum.x;

    float4 s1 = tex2D(sampButterfly, p1);

    p2.x = IN.texcoord_0.x;
    p2.y = abs(s1.x);
    p0.y =     s1.y ;
    p0.x = IN.texcoord_0.x;

    float4 s2 = tex2D(sampSourceImage, p2);
    float4 s0 = tex2D(sampSourceImage, p0);
    float2 r1, r3;

    r3.x = (s1.z * s2.x) - (s1.w * s2.w);
    r3.y = (s1.w * s2.x) + (s1.z * s2.w);

    r1.xy = (s1.x >= 0.0 ? -r3 : r3);

    r1.x = (s0.x + r1.x);
    r1.y = (s0.w + r1.y);

    OUT.color_0.xyz = r1.x;
    OUT.color_0.w = r1.y;

    return OUT;
};

// approximately 19 instruction slots used (3 texture, 16 arithmetic)
